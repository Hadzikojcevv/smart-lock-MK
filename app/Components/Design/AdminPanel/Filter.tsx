import React from 'react'
import FilterBtn from './FilterBtn'



const Filter = () => {

    const handleFilter = (filterBy:string) => {



    }

  return (
    <ul>
        {/* {filters.map(filter => <FilterBtn filter={filter.filter} text={filter.text}/>)} */}
    </ul>
  )
}

export default Filter